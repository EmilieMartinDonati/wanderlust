export const sendRatingForm = (values) => {
  console.log('inACTION', values);
  // créer un pdf avec ... 
}

export const verifyTaggedUser = async (value, userId) => {
  /** vérifier si existe
   * vérifier si fait partie de ses amis
   * vérifie s'il a accepté d'être tagué dans la photo.
   */
  return true;
}