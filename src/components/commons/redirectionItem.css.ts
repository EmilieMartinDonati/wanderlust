import { style } from "@vanilla-extract/css";

export const redirectionItemStyle = style({
  display: "flex",
})

export const redirectionItemMain = style({
  flexGrow: 3,
})

// export const redirectionItem